meta {
  name: Reset streaks
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/v2/rpc/RPC_ID_STREAKS_RESET
  body: json
  auth: inherit
}

body:json {
  {
    "ids": ["daily_login", "consecutive_wins", "weekly_challenge"]
  }
}
