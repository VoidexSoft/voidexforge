meta {
  name: Claim streak rewards
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/v2/rpc/RPC_ID_STREAKS_CLAIM
  body: json
  auth: inherit
}

body:json {
  {
    "ids": ["daily_login", "consecutive_wins"]
  }
}
