meta {
  name: Get event leaderboard
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/v2/rpc/RPC_ID_EVENT_LEADERBOARD_GET
  body: json
  auth: inherit
}

body:json {
  {
    "id": "weekly_tournament_01"
  }
}
