meta {
  name: List event leaderboards
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/v2/rpc/RPC_ID_EVENT_LEADERBOARD_LIST
  body: json
  auth: inherit
}

body:json {
  {
    "with_scores": true,
    "categories": ["tournament", "daily_challenge"]
  }
}
