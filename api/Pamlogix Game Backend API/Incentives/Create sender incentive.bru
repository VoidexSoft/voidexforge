meta {
  name: Create sender incentive
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/v2/rpc/RPC_ID_INCENTIVES_SENDER_CREATE
  body: json
  auth: inherit
}

body:json {
  {
    "id": "welcome_bonus_config"
  }
}
